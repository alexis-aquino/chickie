# Local MySQL connection — create the database and load schema.sql first:
#   mysql -u root -e "CREATE DATABASE IF NOT EXISTS chickie"
#   mysql -u root chickie < schema.sql
DATABASE_URL=mysql+pymysql://root:@localhost:3306/chickie
